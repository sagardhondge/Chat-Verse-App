# MongoDB connection string (✔️ Valid)
MONGO_URI=mongodb+srv://chatuser:SAGAR7890@chatverse.oa8aapx.mongodb.net/chatdb?retryWrites=true&w=majority

# JWT secret key (✔️ Important: should match frontend token decoding logic if used)
JWT_SECRET=your-secret

# Allowed frontend origin (✔️ used in CORS)
CLIENT_URL=https://chatverseapp.vercel.app

# (Optional) Add localhost for local testing too
LOCAL_CLIENT_URL=http://localhost:5173
